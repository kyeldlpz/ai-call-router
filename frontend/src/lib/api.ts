/**
 * REST API client functions — scaffolding only.
 *
 * TODO: T6/T14 — Implement:
 * - createCall(scenario?) → ApiResponse<CallCreatedData>
 * - getCall(callId) → ApiResponse<CallData>
 * - endCall(callId) → ApiResponse<CallData>
 */

const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";

export { API_BASE };
