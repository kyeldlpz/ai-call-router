/**
 * WebSocket connection hook — scaffolding only.
 *
 * TODO: T11 — Implement:
 * - Connect to /ws/v1/call/{call_id}
 * - Parse incoming messages by type
 * - Provide send() method
 * - Reconnection with exponential backoff
 * - Clean disconnect on unmount
 */
