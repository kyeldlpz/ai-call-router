/**
 * Call management hook — scaffolding only.
 *
 * TODO: T14 — Implement:
 * - startCall(): POST /api/v1/calls → WebSocket → audio capture
 * - endCall(): send call_end → stop audio → update state
 * - Route WS messages to reducer and audio playback
 * - Duration timer management
 */
