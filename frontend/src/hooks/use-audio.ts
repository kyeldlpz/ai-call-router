/**
 * Browser audio capture hook — scaffolding only.
 *
 * TODO: T8 — Implement:
 * - getUserMedia for microphone access
 * - AudioWorklet for PCM16 capture at 24kHz
 * - Base64 encode and emit audio chunks
 * - startCapture() / stopCapture() methods
 * - Permission denial handling
 */
